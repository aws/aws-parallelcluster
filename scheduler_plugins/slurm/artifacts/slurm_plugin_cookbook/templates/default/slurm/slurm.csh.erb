#
# slurm.csh:
#     Sets the C shell user environment for slurm commands
#
set path = ($path <%= node['slurm']['install_dir'] %>/bin)
if ( ${?MANPATH} ) then
  setenv MANPATH ${MANPATH}:<%= node['slurm']['install_dir'] %>/share/man
else
  setenv MANPATH :<%= node['slurm']['install_dir'] %>/share/man
endif
