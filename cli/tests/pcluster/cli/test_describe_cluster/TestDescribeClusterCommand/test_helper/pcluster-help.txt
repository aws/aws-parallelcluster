usage: pcluster describe-cluster [-h] -n CLUSTER_NAME [-r REGION]
                                 [--verbose VERBOSE] [--debug] [--query QUERY]

Get detailed information about an existing cluster.

options:
  -h, --help            show this help message and exit
  -n CLUSTER_NAME, --cluster-name CLUSTER_NAME
                        Name of the cluster
  -r REGION, --region REGION
                        AWS Region that the operation corresponds to.
  --verbose VERBOSE     A flag that provides more detailed output about the
                        cluster.
  --debug               Turn on debug logging.
  --query QUERY         JMESPath query to perform on output.
