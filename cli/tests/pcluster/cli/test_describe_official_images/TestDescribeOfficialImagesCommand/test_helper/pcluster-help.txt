usage: pcluster describe-official-images [-h] [--region REGION] [--os OS]
                                         [--architecture ARCHITECTURE]
                                         [--debug]

Describe ParallelCluster AMIs.

optional arguments:
  -h, --help            show this help message and exit
  --region REGION       AWS Region that the operation corresponds to.
  --os OS               Filter by OS distribution (Default is to not filter.)
  --architecture ARCHITECTURE
                        Filter by architecture (Default is to not filter.)
  --debug               Turn on debug logging.
