#!/bin/bash
# script whose only purpose is to not work
if [[[[[]]]"${instance_type}" == "c5.large" ]}}}}}}}; then
  ecasdfho "Test Bootstrap error that causes instance to self terminate."
  exit 1
fi
