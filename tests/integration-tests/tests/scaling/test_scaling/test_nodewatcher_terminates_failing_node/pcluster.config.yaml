Image:
  Os: {{ os }}
HeadNode:
  InstanceType: {{ instance }}
  Networking:
    SubnetId: {{ public_subnet_id }}
  Ssh:
    KeyName: {{ key_name }}
Scheduling:
  {{ scheduler }}:
    Queues:
      - Name: queue-0
        ComputeResources:
          - Name: compute-resource-0
            InstanceType: {{ instance }}
            MinCount: {{ initial_queue_size }}
            MaxCount: {{ initial_queue_size }}
        Networking:
          SubnetIds:
            - {{ public_subnet_id }}