Image:
    RootVolume:
        Size: 200
        Encrypted: True

Build:
    Iam:
        InstanceRole: {{ instance_role }}
    InstanceType: {{ instance }}
    ParentImage: {{ parent_image }}

CustomS3Bucket: {{ bucket_name }}
